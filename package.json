{
  "name": "klinik-core-sdk",
  "version": "1.0.0",
  "description": "Generic utilities for Klinik Management System",
  "module": "src/index.ts",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node && tsc --emitDeclarationOnly --declaration --outDir dist",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "drizzle-orm": ">=0.30.0"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^8.4.0",
    "drizzle-orm": "^0.45.1"
  },
  "files": [
    "src"
  ],
  "keywords": [
    "hospital",
    "sdk",
    "api",
    "healthcare"
  ],
  "license": "MIT"
}
